# 迁移到新电脑 — 操作手册

> 生成日期：2026-08-29
> 策略：**文本/代码走 GitHub 私有仓库 `ContentCrawlKits`；Claude Code 环境与大型媒体走本地打包**。平衡切换、最小损失、最少时间。

## 一、仓库里有什么（旧机已做）

- 项目全部文本：`CLAUDE.md`、`docs/`、`1.设计思路及规划/`、`scripts/` 源码、`HSEE-Content-Script.txt`、`script.txt`
- 记忆快照：`_claude-memory/`（从旧机 auto-memory 复制；换机后拷回 `~/.claude/projects/<key>/memory/`）
- 排除项：`.claude/settings.local.json`（含绝对路径）、`*.mp4` 等媒体（走本地备份包）、`scripts/output/`

## 二、旧机 → 新机步骤

### 1. 新机装环境
| 工具 | 用途 | 获取 |
|---|---|---|
| git（含 Git Bash） | 拉代码 | https://git-scm.com |
| Node.js LTS | MCP 的 npx / Claude Code | https://nodejs.org |
| Python / conda | `scripts/` 里 Pillow、numpy 等 | https://www.anaconda.com |
| Claude Code | 主工具 | `npm install -g @anthropic-ai/claude-code` |
| 剪映 / 腾讯智影 / 蚁小二 等 | 视频生产分发 | 官网 |

### 2. 拉代码
```bash
git clone https://github.com/<你的账号>/ContentCrawlKits.git
```

记忆目录改名：`~/.claude/projects/` 下的目录名由项目路径编码生成（`E:\1.HSEE\6.ContentCrawlKits` → `E--1-HSEE-6-ContentCrawlKits`）。**新机项目路径不同则目录名不同**，把 `_claude-memory/` 内容拷到新目录名下的 `memory/`。

### 3. 还原 Claude Code 环境
旧机备份包在 `E:\1.HSEE\迁移备份\`，解压 `claude-env-<日期>.tar.gz`：

| 包内路径 | 还原到 |
|---|---|
| `claude-config/settings.json` | `~/.claude/settings.json`（**改 DeepSeek token**） |
| `claude-config/settings.local.json` | `~/.claude/settings.local.json` |
| `claude-config/agents/` | `~/.claude/agents/` |
| `claude-config/skills/` + `agents-skills/` | `~/.claude/skills/`、`~/.agents/skills/` |
| `claude-config/plugins/`（json + data） | `~/.claude/plugins/` |
| `claude-config/.mcp.json` | `~/.claude/.mcp.json` |
| `claude-config/rules/` | `~/.claude/rules/` |
| `claude-config/claude-json-full-backup.json` | MCP 服务器配置参考（含各服务器定义） |
| `project-memory/memory/` | `~/.claude/projects/<新key>/memory/`（与 `_claude-memory` 二选一） |
| `project-sessions/*.jsonl` | `~/.claude/projects/<新key>/`（会话历史，claude-mem 数据源） |

### 4. 重配密钥（三处，勿入 git）
1. `~/.claude/settings.json` → `env.ANTHROPIC_AUTH_TOKEN` = DeepSeek API key（含 `ANTHROPIC_BASE_URL`、`ANTHROPIC_MODEL`）
2. MCP `firecrawl` → `FIRECRAWL_API_KEY`
3. MCP `context7` → `CONTEXT7_API_KEY`

可用 `claude mcp add` 重建，或直接沿用备份里的定义（`~/.claude.json`）。

### 5. 媒体还原（可选）
解压 `project-videos-<日期>.tar.gz` 到项目根，mp4 会回到 docs 对应位置。

### 6. 验证
新开会话确认：记忆加载 ✓、技能列表 ✓、MCP 服务器 ✓、模型 ✓。再跑一次 `scripts/` 里的图像脚本验证 Python 环境。

## 三、换机期间并行工作规则

- **日常提交一条命令**：`bash sync.sh "改动说明"` —— 自动刷新 `_claude-memory/` → commit → push，内容层（含记忆快照）一步同步本地 git + GitHub。
- 或手动：每次改动先 `git add . && git commit` 再 push；换机前 pull。
- 记忆同步以 `_claude-memory/` 为准：在某一台机上把最新 `memory/` 拷回 `_claude-memory/` 并 commit，避免两台机各自演进、互相覆盖。
- GitHub push 用 **PAT**：github.com → Settings → Developer settings → Personal access tokens → Generate new token（勾选 `repo` 权限），克隆/推送时作为密码输入。

## 四、已知坑

1. **auto-memory 目录名 = 项目路径编码**，新机路径不同需改名拷贝。
2. **`settings.local.json` 全是绝对路径/权限规则**（`C:\Users\weiwei\...`），跨机失效，需新机重新授权。
3. **GitHub 插件 MCP 曾报 400**（Authorization header 格式错误），`gh` CLI 未装：修授权或用 PAT 直接 push。
4. **mp4 不入 git**：若想让视频入库，启用 Git LFS（`git lfs track "*.mp4"`）替代。
